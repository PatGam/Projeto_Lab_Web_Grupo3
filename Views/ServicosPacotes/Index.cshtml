@model IEnumerable<Projeto_Lab_Web_Grupo3.Models.ServicosPacotes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pacote)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Servico)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pacote.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Servico.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SevicoPacoteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SevicoPacoteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SevicoPacoteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
