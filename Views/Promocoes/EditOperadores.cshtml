@model PromocoesPacotesViewModel

@{
    ViewData["Title"] = "Editar";
    Layout = "_LayoutAdmin";
}

<section id="main-content">
    <section class="wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h3 class="page-header"><i class="icon_cart"></i>Promoções</h3>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        Atribuir promoção a pacotes e distritos
                    </header>
                    <div class="panel-body">
                        <form asp-action="EditOperadores" class="form-horizontal">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <input type="hidden" asp-for="PromocoesId" />
                            <div class="form-group">
                                <label asp-for="Nome" class="col-sm-2 control-label"></label>
                                <div class="col-sm-10">
                                    <input id="disabledInput" asp-for="Nome" class="form-control" />
                                   
                                </div>
                            </div>
                            
                            <div class="form-group">
                                <label class="control-label col-lg-2" for="inputSuccess">Pacotes com a promoção</label>
                                <div class="col-lg-10">
                                    @for (int i = 0; i <  Model.ListaPacotes.Count; i++)
                                    {
                                        <div class="checkbox">
                                            <label>
                                                <input type="checkbox" value="">
                                                @Html.DisplayFor(z => Model.ListaPacotes[i].NomePacote)
                                            </label>
                                            @Html.HiddenFor(x => Model.ListaPacotes[i].Id)
                                            @Html.CheckBoxFor(z => Model.ListaPacotes[i].Selecionado)
                                        </div>
                                    }
                                </div>
                            </div>
                         
                            <div class="form-group">
                                <div class="col-lg-offset-2 col-lg-10">
                                    <input type="submit" class="btn btn-success" value="Guardar" />
                                    <a class="btn btn-danger" asp-action="Index">Cancelar</a>
                                </div>
                            </div>
                        </form>
                    </div>
                </section>
            </div>
        </div>
    </section>
</section>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
